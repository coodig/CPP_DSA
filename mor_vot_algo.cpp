#include <iostream>
#include <vector>
using namespace std;

vector<int> pairsum(vector<int> num, int target)
{
    vector<int> ans;
    int n = num.size();
    for (int i = 0; i < n; i++)
    {
        for (int j = n + 1; j < n; j++)
        {
        }
    }
}
int main()
{

    // pair sum
    int size;
    cout << "enter the size of the array:";
    cin >> size;
    int array[size] = {};
}